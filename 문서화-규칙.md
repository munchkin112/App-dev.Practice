# 도서 관리 API 문서화 규칙

## 1. 문서화 작업의 범위

- 문서화 작업에서는 엔드포인트 데코레이터, 독스트링, Pydantic 스키마 문서 정보만 다룬다.
- 함수 본문의 처리 로직은 수정하지 않는다.
- `main.py`에 선언된 엔드포인트의 순서를 변경하지 않는다.
- `/books/search`, `/books/filter`, `/books/page`, `/books/external`, `/books/from-external` 같은 리터럴 경로는 `/books/{book_id}`보다 앞에 둔 현재 순서를 유지한다.

## 2. API 태그

`tags_metadata`에 정의된 다음 세 태그만 사용한다.

| 태그 | 설명 | 대상 엔드포인트 |
|---|---|---|
| `도서` | 도서 등록, 조회, 검색 | `GET /books`, `POST /books`, `GET /books/search`, `GET /books/filter`, `GET /books/page`, `GET /books/{book_id}` |
| `외부 연동` | Google Books와 날씨 API 연동 | `GET /books/external`, `POST /books/from-external`, `GET /weather` |
| `시스템` | 서버 상태 확인 | `GET /`, `GET /health`, `GET /info` |

- 각 엔드포인트 데코레이터에는 위 목록에 맞는 `tags=["..."]`를 지정한다.
- 태그 이름의 띄어쓰기를 유지한다. 특히 `외부 연동`을 `외부연동`으로 바꾸지 않는다.

## 3. Summary 작성 규칙

- 모든 엔드포인트에 짧은 한국어 `summary`를 작성한다.
- 현재 코드처럼 대상과 동작이 바로 드러나는 명사형 표현을 사용한다.
- 사용 중인 표현은 다음과 같다.

| 엔드포인트 | Summary |
|---|---|
| `GET /` | API 홈 화면 조회 |
| `GET /health` | 서버 상태 확인 |
| `GET /info` | API 정보 조회 |
| `GET /books` | 도서 목록 조회 |
| `POST /books` | 도서 등록 |
| `GET /books/search` | 도서 제목 검색 |
| `GET /books/filter` | 도서 목록 필터링 |
| `GET /books/page` | 도서 목록 페이지 조회 |
| `GET /books/external` | 외부 도서 검색 |
| `POST /books/from-external` | 외부 도서 등록 |
| `GET /books/{book_id}` | 도서 상세 조회 |
| `GET /weather` | 현재 날씨 조회 |

## 4. 독스트링 작성 규칙

- 모든 엔드포인트 함수에 독스트링을 작성한다.
- 첫 문장은 해당 함수가 반환하거나 수행하는 동작을 한국어 문장으로 설명한다.
- 추가 동작이 있는 경우 빈 줄 뒤에 조건을 설명한다.
- 실제 함수 파라미터를 설명할 때는 코드와 동일한 이름을 백틱으로 표시한다. 현재 사용 중인 예는 `author`, `sort`, `skip`, `limit`, `fallback`이다.
- 요청 본문을 설명할 때는 실제 Pydantic 필드명을 사용한다. `BookCreate`의 현재 필드는 `title`, `author`, `year`, `tags`, `publisher`이다.
- 기본값에 따른 동작, 외부 요청 실패 시 fallback 동작, 실제로 반환하는 HTTP 오류만 설명한다.

## 5. Responses 작성 규칙

- 함수 본문에 실제 `raise HTTPException(status_code=N, detail="...")`가 있는 상태 코드만 데코레이터의 `responses`에 추가한다.
- `description`에는 실제 `detail` 문자열을 그대로 사용한다.
- 현재 명시적으로 문서화하는 오류 응답은 다음과 같다.

| 엔드포인트 | 상태 코드 | Description |
|---|---:|---|
| `POST /books` | 409 | 중복된 도서입니다. |
| `GET /books/external` | 504 | 외부 API 응답이 지연됩니다 |
| `GET /books/external` | 502 | 외부 API가 오류를 반환했습니다 / 외부 API에 연결할 수 없습니다 |
| `POST /books/from-external` | 409 | 이미 등록된 제목입니다 |
| `GET /books/{book_id}` | 404 | 도서를 찾을 수 없습니다 |

- 같은 엔드포인트에서 같은 상태 코드로 여러 `detail`을 반환하면 하나의 상태 코드 항목에 실제 문자열을 모두 기록한다. 현재 `GET /books/external`의 502가 이에 해당한다.
- 실제 `raise`가 사라지면 해당 `responses`도 제거한다.
- 성공 응답과 FastAPI가 자동 생성하는 검증 응답을 `responses`에 중복해서 추가하지 않는다.
- 기존 `response_model`, `status_code`, `response_description`, `tags`, `summary`는 유지한다.

## 6. Pydantic Field 문서화 규칙

- 각 모델 필드는 `Field`의 `description`과 `examples`로 문서화한다.
- `description`은 `도서 제목`, `출판 연도`, `저자 목록`처럼 짧고 명확한 한국어 명사형으로 작성한다.
- `examples`에는 현재 API에서 사용할 법한 값을 넣는다.
- 현재 사용하는 예시는 도서명 `처음 시작하는 FastAPI`, 저자 `빌 루바노빅`, 출판 연도 `2025`, 태그 `Python`·`웹 개발`, 출판사 `한빛미디어`, 소재지 `서울`이다.
- 외부 도서 예시는 `Do it! 점프 투 파이썬`, 저자 `박응용`, 출판일 `2019`를 사용한다.
- 날씨 예시는 위도 `37.5665`, 경도 `126.978`, 현재 기온 `24.3`, 관측 시각 `2026-08-18T14:00`을 사용한다.
- 기존 `min_length`, `max_length`, `ge`, `le` 제약은 변경하지 않는다.
- 기존 필드 이름과 타입은 변경하지 않는다.
- `BookCreate`의 `model_config`에는 Swagger 요청 본문용 전체 `json_schema_extra` 예시를 유지한다.

## 7. Optional 필드와 기본값

- 문서화를 위해 `Field`로 바꾸더라도 기존 기본값을 유지한다.
- `Publisher | None`과 같은 Optional 필드는 `Field(default=None, ...)`처럼 `default=None`을 명시한다.
- `default_factory=list`를 사용하는 목록 필드는 그대로 유지한다.
- 현재 유지해야 하는 기본값은 다음과 같다.

| 모델 | 필드 | 기본값 |
|---|---|---|
| `BookCreate` | `tags` | `default_factory=list` |
| `BookCreate` | `publisher` | `default=None` |
| `GoogleBooks` | `authors` | `default_factory=list` |
| `GoogleBooks` | `published_date` | `""` |
| `ExternalBook` | `authors` | `default_factory=list` |
| `ExternalBook` | `published_date` | `""` |

## 8. Validator 보존

- `BookCreate.strip_title`의 `field_validator`는 문서화 작업에서 수정하지 않는다.
- `@field_validator("title")`, `@classmethod`, 공백 제거, 빈 제목 검사, 반환 로직을 그대로 유지한다.

## 9. 문서 검증 체크리스트

작업 후 서버를 실행하고 다음 문서를 모두 확인한다.

### `/docs`

- `도서`, `외부 연동`, `시스템` 태그와 각 엔드포인트 분류를 확인한다.
- 각 엔드포인트의 summary와 독스트링 내용을 확인한다.
- 요청 모델의 description, examples, 제약조건과 `BookCreate` 전체 요청 예시를 확인한다.
- 실제 `raise HTTPException`과 문서의 오류 상태 코드가 일치하는지 확인한다.

### `/redoc`

- 세 태그의 이름과 설명을 확인한다.
- 엔드포인트 summary, 독스트링, 요청·응답 모델 설명을 확인한다.
- 리터럴 경로가 `/books/{book_id}`와 잘못 합쳐지지 않았는지 확인한다.

### `/openapi.json`

- `tags`에 `도서`, `외부 연동`, `시스템`이 정의돼 있는지 확인한다.
- 각 operation의 `tags`, `summary`, `description`, `responses`를 실제 코드와 대조한다.
- Pydantic schema의 `description`, `examples`, 필수 필드, 기본값, 길이·범위 제약을 확인한다.
- 문서화하지 않은 오류 상태 코드나 실제 `raise`가 없는 오류 상태 코드가 없는지 확인한다.

## 10. Postman 회귀 테스트

문서화 작업 후 현재 엔드포인트에 대해 다음 요청을 다시 실행한다.

| 분류 | 요청 | 확인 항목 |
|---|---|---|
| 시스템 | `GET /` | 정적 홈 화면 응답 |
| 시스템 | `GET /health` | `status`가 `ok`인 응답 |
| 시스템 | `GET /info` | API 이름과 버전 응답 |
| 도서 | `GET /books` | 전체 도서 목록과 `BookResponse` 형식 |
| 도서 | `POST /books` | 정상 등록 시 201과 등록된 도서 정보 |
| 도서 | `POST /books` | 중복 제목 등록 시 409와 `중복된 도서입니다.` |
| 도서 | `GET /books/search` | 검색어 포함 결과와 빈 검색어의 전체 목록 |
| 도서 | `GET /books/filter` | `author` 필터와 `sort=year` 정렬 |
| 도서 | `GET /books/page` | `skip`, `limit`에 따른 목록 범위 |
| 외부 연동 | `GET /books/external` | 정상 검색 결과의 `title`, `authors`, `published_date` |
| 외부 연동 | `GET /books/external` | 외부 요청 실패 시 502·504 또는 `fallback=true`의 대체 목록 |
| 외부 연동 | `POST /books/from-external` | 정상 등록 시 201과 내부 `BookResponse` 변환 결과 |
| 외부 연동 | `POST /books/from-external` | 중복 제목 등록 시 409와 `이미 등록된 제목입니다` |
| 도서 | `GET /books/{book_id}` | 존재하는 번호의 도서 정보와 없는 번호의 404 |
| 외부 연동 | `GET /weather` | 위도·경도에 따른 `WeatherResponse` 형식 |

- 문서화 작업 전후에 동일한 요청의 상태 코드와 응답 본문 구조가 유지되는지 비교한다.
- `/books/from-external`과 다른 리터럴 경로가 `/books/{book_id}`로 처리되지 않는지 확인한다.
